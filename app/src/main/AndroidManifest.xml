<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 🌐 INTERNET (Firebase required) -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- ☎️ CALL USER (Admin dial feature) -->
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UniCanteen"
        tools:targetApi="31">

        <!-- 🚀 SPLASH (LAUNCHER) -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 👤 USER ACTIVITIES -->
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.DashboardActivity" />
        <activity android:name=".activities.MenuActivity" />
        <activity android:name=".activities.CartActivity" />
        <activity android:name=".activities.LocationActivity" />
        <activity android:name=".activities.PaymentActivity" />
        <activity android:name=".activities.PlaceOrderActivity" />
        <activity android:name=".activities.OrderHistoryActivity" />
        <activity android:name=".activities.UserOrdersActivity" />

        <!-- 🔐 ADMIN ACTIVITIES -->
        <activity android:name=".activities.AdminLoginActivity" />
        <activity android:name=".activities.AdminDashboardActivity" />
        <activity android:name=".activities.AdminOrdersActivity" />
        <activity android:name=".activities.AdminManageItemsActivity" />

    </application>
</manifest>

<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.UniCanteen" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Color palette custom properties -->
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/primary_blue_light</item>
        <item name="colorOnPrimaryContainer">@color/primary_blue_dark</item>
        <item name="colorSecondary">@color/accent_cyan</item>
        <item name="android:windowBackground">@color/bg_light</item>

        <!-- Status Bar -->
        <item name="android:statusBarColor">@color/bg_dark</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.UniCanteen" parent="Base.Theme.UniCanteen" />
</resources>